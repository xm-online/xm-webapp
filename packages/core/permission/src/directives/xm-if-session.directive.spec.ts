import { XmIfSessionDirective } from './xm-if-session.directive';

describe('IfSessionDirective', () => {
    it('should create an instance', () => {
        const directive = new XmIfSessionDirective(null, null, null);
        expect(directive).toBeTruthy();
    });
});
