import { StringHandlerDirective } from './string-handler.directive';

describe('StringHandlerDirective', () => {
    it('should create an instance', () => {
        const directive = new StringHandlerDirective(null);
        expect(directive).toBeTruthy();
    });
});
