import { DomainDirective } from './domain.directive';

describe('DomainDirective', () => {
    it('should create an instance', () => {
        const directive = new DomainDirective(null, null);
        expect(directive).toBeTruthy();
    });
});
