import { GuestBackgroundDirective } from './guest-background.directive';

describe('GuestBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new GuestBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
