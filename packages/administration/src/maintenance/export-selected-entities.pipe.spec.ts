import { ExportSelectedEntitiesPipe } from './export-selected-entities.pipe';

describe('ExportSelectedEntitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExportSelectedEntitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
